import { strategy } from '../login-middleware'

describe('login-middleware', () => {
  it('Should create JWT token', () => {
    expect(false).toBeTruthy()
  })
  it('Should fetch user from db', () => {
    expect(false).toBeTruthy()
  })
})
